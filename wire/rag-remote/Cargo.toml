[package]
name = "rag-remote"
description = "Generic Remote<T> adapters: implement rag-contracts traits by speaking gRPC."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

# Implements the domain traits (`rag-contracts`) over the wire (`rag-proto`),
# converting domain <-> protobuf. `tonic` provides the client transport.
[dependencies]
rag-contracts = { workspace = true }
rag-types = { workspace = true }
rag-proto = { workspace = true }
tonic = { workspace = true }
